import React, { VFC } from 'react';
import Workspace from '@layouts/Workspace';

const Channel: VFC = () => {
  return (
    <Workspace>
      <div>로그인되었습니다!</div>
    </Workspace>
  );
};

export default Channel;
